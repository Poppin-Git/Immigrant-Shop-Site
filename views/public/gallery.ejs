<h1><%= title %></h1>

<div class="gallery">
  <% images.forEach(image => { %>
    <div class="gallery-card public-card">
      <img src="/images/<%= image %>" alt="Gallery Image">
    </div>
  <% }); %>
</div>
